import {
    Typography,
} from "@mui/material";

export function Dashboard(){

    return(
        <Typography component="h6" variant="h6">
                dashboard
            </Typography>
    );
}